.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background-color: #3e686f; /* Updated to requested color */
  padding: 1rem 2rem;
  color: white;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

.logo {
  font-size: 1.8rem;
  font-weight: bold;
  cursor: pointer;
  color: white;
}

.connectionIndicator {
  font-size: 1rem;
  font-weight: bold;
  color: white;
  text-align: center;
  margin: 0 1rem;
}

.connected {
  color: #00ff00; /* Green */
}

.disconnected {
  color: #808080; /* Grey */
}

.navLinks {
  list-style: none;
  display: flex;
  gap: 1.5rem;
  justify-content: flex-end;
  align-items: center;
}

.navLinks li a {
  color: white;
  text-decoration: none;
  font-size: 1rem;
  transition: color 0.3s ease;
}

.navLinks li a:hover {
  color: #aad1da; /* Lighter greyish blue */
  text-decoration: underline;
}

.gearIcon {
  font-size: 1.5rem;
  cursor: pointer;
  color: white;
  transition: transform 0.2s ease, color 0.3s ease;
}

.gearIcon:hover {
  transform: rotate(20deg);
  color: #aad1da;
}

.profile {
  cursor: pointer;
  transition: transform 0.2s ease;
}

.profile:hover {
  transform: scale(1.1);
}

.avatar {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  border: 2px solid #aad1da;
}

.placeholderAvatar {
  width: 40px;
  height: 40px;
  background-color: #ccc;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #333;
  font-size: 1.2rem;
  border: 2px solid #aad1da;
}

/* Media Query for Responsiveness */
@media (max-width: 768px) {
  .navbar {
      flex-wrap: wrap;
      padding: 1rem;
  }

  .navLinks {
      flex-direction: column;
      gap: 1rem;
  }
}
